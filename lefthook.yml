# EXAMPLE USAGE:
#
#   Refer for explanation to following link:
#   https://lefthook.dev/configuration/

check:
  paralle: true
  commands:
    test:
      run: yarn vitest -w false
    tsc:
      run: tsc --noEmit -p .

pre-commit:
  parallel: true
  jobs:
    - run: yarn t

commit-msg:
  commands:
    commitlint:
      run: npx --yes commitlint --edit
